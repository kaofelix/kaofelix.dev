---
const { src, caption } = Astro.props;
---

<div class="flex flex-col items-center justify-center">
  <video src={src} autoplay loop playsinline></video>
  {
    caption && (
      <figcaption class="text-center text-sm text-gray-500 mt-1">
        {caption}
      </figcaption>
    )
  }
</div>
